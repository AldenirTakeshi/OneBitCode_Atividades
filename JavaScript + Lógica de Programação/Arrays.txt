// Adicionar
1- .Push() Adicionar um elemento no final do arrays!
2- .Unshift() Adicionar no começo do array!

//Remover
3- .Pop() Remove o ultimo e retorna o elemento!
4- .Shift() Remover o primeiro elemento do array!

//Pesquisar um Elemento
5- .includes('pesquisa') devolve boolean
6- .indexOf('pesquisa') devolve o indice

//Cortar e concatenar
7- .slice() Cortar o array// Faz uma cópia
8- .concat() Concatenar com outros elementos ou arrays.

//Substituição dos elementos/ por exemplo substituir por null!
9- .splice(indice,1,'Gandalf o cinzento') Remover um grupo e substituir por outros elementos
retorna o elemento removido caso salvo em uma variavel.
--Devolve uma lista 

//Iterar sobre os elementodo arrays
10- for(let i = 0; i < sociedade.length; i++ ){
  let elemento = sociedade.[i]
}
console.log(elemento + 'se encontra na posição ' + i)
